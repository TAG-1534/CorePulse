version: '3.8'
services:
  dashboard:
    build: 
      context: .
    ports:
      - "8085:5000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      - TRUENAS_IP=192.168.1.X  # Your NAS IP
      - TRUENAS_API_KEY=your_key_here
    restart: always
